{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "echo 'Serverless function build handled by Vercel'",
  "installCommand": "NODE_ENV=development HUSKY=0 pnpm install --frozen-lockfile=false --ignore-scripts && pnpm prisma generate",
  "functions": {
    "api/index.ts": {
      "memory": 1024,
      "maxDuration": 60
    }
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index" }]
}
