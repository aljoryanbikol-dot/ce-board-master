# Backend API base (browser-visible). In dev, the Next rewrite proxies /api/backend → API.
NEXT_PUBLIC_API_URL=/api/backend
# Server-side proxy target for the dev rewrite.
API_PROXY_TARGET=http://localhost:3001
NEXT_PUBLIC_APP_NAME=CE Board Master
